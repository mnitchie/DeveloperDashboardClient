import http from 'developer-dashboard-client/utils/http';
import { module, test } from 'qunit';

module('Unit | Utility | http');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = http();
  assert.ok(result);
});
